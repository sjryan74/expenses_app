<h1>Transactions</h1>

<p><%= link_to "New Transaction", new_transaction_path, class: "crud_button" %></p>

<div class="table-responsive">
  <table class="table table-hover table-bordered table-sm">
    <thead class="thead-light">
      <tr>
        <th>Date</th>
        <th>Amount</th>
        <th>Description</th>
      </tr>
    </thead>
    <body>
      <% @transactions.each do |trx| %>
      <tr>
        <td><%= trx.trans_date %></td>
        <td><%= number_to_currency(trx.amount, unit: '') %></td>
        <td><%= trx.description %></td>
      </tr>
      <% end %>
    </body>
  </table>
</div>


